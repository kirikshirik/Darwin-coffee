# Darwin Coffee — backend (Python)
# Ядро MVP: модели данных + расчёт прибыли.
SQLAlchemy>=2.0,<3.0
python-dotenv>=1.0

# Добавим по мере роста проекта:
# httpx>=0.27       # интеграция с Эвотор API (EvotorClient)
# aiogram>=3.0      # Telegram-бот
# alembic>=1.13     # миграции БД
# psycopg[binary]>=3.1  # драйвер PostgreSQL (на проде вместо SQLite)
