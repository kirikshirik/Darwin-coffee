# Darwin Coffee — backend (Python)
# Ядро MVP: модели данных + расчёт прибыли.
SQLAlchemy>=2.0,<3.0
python-dotenv>=1.0

# Интеграция с Эвотор API (EvotorClient) — async HTTP.
httpx>=0.27

# Telegram-бот + планировщик ежедневных отчётов.
aiogram>=3.4,<4.0
APScheduler>=3.10,<4.0

# HTTP health-сервер для PaaS (Render) — см. backend/bot/health.py. aiogram и так
# тянет aiohttp, но указываем явно, раз импортируем его напрямую.
aiohttp>=3.9

# Драйвер PostgreSQL — для облачного деплоя без персистентного диска (Render + Neon),
# где SQLite не годится. Активируется переменной DATABASE_URL (см. db.py, deploy/RENDER.md).
psycopg[binary]>=3.1

# Таймзоны для zoneinfo (MSK): на минимальном Linux-VPS системной tz-базы может
# не быть — без неё ZoneInfo("Europe/Moscow") падает (mapping.py, scheduler.py).
tzdata>=2024.1

# Добавим по мере роста проекта:
# alembic>=1.13         # миграции БД
