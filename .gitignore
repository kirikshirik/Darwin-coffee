node_modules
dist
# собранный Mini App коммитится целиком: на Render (runtime: python) нет npm,
# и без этого исключения JS/CSS-бандлы выпадали из деплоя (в git был только index.html)
!frontend/dist/
.env
.DS_Store

# Python
.venv/
__pycache__/
*.pyc

# Локальная БД + WAL/SHM + бэкапы (на проде живёт в /opt/darwin, см. DEPLOY.md)
darwin.db
darwin.db-wal
darwin.db-shm
darwin.backup.db

# Неактивные в приложении исходники/черновики (Excel, .docx, брейншторм, html-черновики,
# сводки). Цифры из них уже перенесены в код (darwin_data/cost_reference/actuals_data).
# Лежат локально для справки, но в общих коммитах не участвуют.
/reference/

# Docker локальная директория данных
/data/
